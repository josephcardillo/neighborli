.section
  .container
    %h2 Users
    %ul.collection
      - @users.each do |user|
        %li.collection-item
          = link_to(user.fname + " " + user.lname, user_path(user) )
          %a{href: "mailto:#{user.email}"}= user.email